name: Android CI

on:
  push:
    branches: [ master ]
    tags:
      - zee*/vc*/vn*
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: gradle
    - name: Get Tag
      run: echo "tagValue=${{github.ref_name}}" >> $GITHUB_ENV
    - name: Print Tag
      run: echo ${{env.tagValue}}
    - name: Is DC Build
      run: |
        if [[ ${{env.tagValue}} =~ "dcbuild" ]]; then
          echo "isDCBuild=1" >> $GITHUB_ENV
        else 
          echo "isDCBuild=0" >> $GITHUB_ENV
        fi
    - name: Print DC Build
      run: echo ${{env.isDCBuild}}
